# MDViewer 런타임 의존성 (Python 3.13 호환)
#
# 버전 정책:
#   - PySide6 는 메이저/마이너를 고정 상한으로 두어 패키징 재현성을 확보한다.
#     (Qt WebEngine 동작/번들이 버전에 민감하므로) 6.8+ 권장.
#   - 코어 라이브러리는 하한만 두고 상한은 느슨히 둔다.

# --- GUI (UI 계층 전용) ---
PySide6>=6.8,<6.10        # Qt 공식 바인딩, QWebEngineView 로 HTML 렌더

# --- 마크다운 파싱 (코어 엔진) ---
markdown-it-py>=3.0       # CommonMark 준수 파서, 플러그인 확장 가능
mdit-py-plugins>=0.4      # GFM 확장: 테이블/각주/작업목록/앵커 등

# --- 코드 하이라이트 (코어 엔진) ---
Pygments>=2.17            # 광범위한 언어 지원, CSS 테마 생성

# --- 파일 변경 감시 (코어 엔진) ---
watchdog>=4.0             # 크로스플랫폼 파일시스템 이벤트

# --- 인코딩 자동 감지 (코어 엔진) ---
charset-normalizer>=3.3   # 임의 인코딩 파일 안전 디코딩

# --- HTML → Markdown 변환 (코어 엔진; 클립보드 붙여넣기) ---
html2text>=2024.2.26      # 순수 파이썬, 런타임 무의존 → PyInstaller 번들 안전
